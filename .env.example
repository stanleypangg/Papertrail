# Existing PageWorld env (do not change)
BACKBOARD_API_KEY=
OPENAI_API_KEY=

# Sleuth env (劇本殺 / jubensha murder mystery game)
WLT_API_KEY=                       # World Labs Marble API key (3D world generation)
SLEUTH_MODEL_FAST=                 # Backboard model for NPC turns (e.g. gemini-2.5-flash)
SLEUTH_MODEL_PROSE=                # Backboard model for host narration (e.g. gemini-3-flash-preview)
SLEUTH_WORLD_PROVIDER=worldlabs    # 'worldlabs' for live Marble, 'mock' for offline dev
SLEUTH_SECRET=hackathon-secret     # Gates POST /api/sleuth/worlds/generate
